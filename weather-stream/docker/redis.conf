# Redis Configuration

# Disable automatic snapshots
save ""

# Enable AOF persistence
appendonly yes

# Write to disk after every write operation
appendfsync always

# Enable TCP connections
bind 0.0.0.0
port 6379
